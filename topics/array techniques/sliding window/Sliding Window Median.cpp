#include<bits/stdc++.h>
using namespace std;
signed main(){
    int n, k; cin>>n>>k;
    vector<int> a(n); 
    for(int i = 0; i < n; i++)cin>>a[i];

    set<int> s;
    s.insert(1);
    s.insert(1);
    s.insert(3);
    s.insert(4);
    s.insert(5);
    

    return 0;
}